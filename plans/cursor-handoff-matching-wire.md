# Handoff — Matching extract (SPLIT-UI-MATCH-01)

**Status: MERGED** — modules created and wired into `kundli-app.tsx`.

- `src/engine/matching.ts`
- `src/screens/MatchingScreen.tsx` (`MatchMaker` takes `computeKundli` prop)
