# Festival profiles

One markdown file per festival/vrat key (same key as `FEST_META` / `VRAT_VIDHI`).

**Template:** see `plans/festival-content-profiles.md`.

**Backlog:** Google Sheets **P0 row #29** — Complete festival/vrat direct-page coverage. All profile, timing, copy, image and Hindi-tone work is P0 (no F0–F3 / P1–P3 sub-phases).

**Profiles started:** `diwali`, `karva-chauth`, `chhath` (+ `hartalika-teej` TODO). Calendar fix doc: `ekadashi-ashadha.md`.

Agents: do not edit `src/data/vrat-vidhis.ts` for a festival until its profile’s **Must-show** section is satisfied or explicitly deferred in task-log.
